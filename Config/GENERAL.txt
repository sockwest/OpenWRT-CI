# =================================================================
# 京东云雅典娜 (AX6600) 专属精简编译配置 (最终版)
# 核心架构: 高通硬件加速 + eBPF底层强扣分流 + 轻量化局域网共享
# =================================================================

# -----------------------------------------------------------------
# 1. 核心代理与分流引擎 
# 注释: 底层沙盒物理隔离与主力机精确分流
# -----------------------------------------------------------------
# 开启 Linux 内核 eBPF 和 BTF 支持 (daed 能够挂载并劫持底层流量的先决条件)
CONFIG_KERNEL_BPF_EVENTS=y
CONFIG_KERNEL_CGROUP_BPF=y
CONFIG_KERNEL_DEBUG_INFO=y
CONFIG_KERNEL_DEBUG_INFO_BTF=y

# 引入核心底层劫持引擎 (负责基于 MAC 地址的物理强制扣留)
CONFIG_PACKAGE_luci-app-daed=y
CONFIG_PACKAGE_daed=y

# 双代理引擎容灾备份 (编译入静态文件，运行时分配不同监听端口即可互不干扰)
CONFIG_PACKAGE_sing-box=y
CONFIG_PACKAGE_xray-core=y

# 保留带图形界面的备用代理方案 (不启用时仅为静态文件，不占用端口和算力)
CONFIG_PACKAGE_luci-app-homeproxy=y

# -----------------------------------------------------------------
# 2. 局域网轻量化中转站
# 注释: 满足设备间的无缝文件流转及打印共享，避免过度开放本机权限
# -----------------------------------------------------------------
# 启用标准的 SMB 共享协议 (苹果生态和 Windows 原生支持最好)
CONFIG_PACKAGE_luci-app-samba4=y
# 保留必要的磁盘挂载基础组件
CONFIG_PACKAGE_automount=y
CONFIG_PACKAGE_blkid=y
CONFIG_PACKAGE_lsblk=y
CONFIG_PACKAGE_kmod-usb-storage=y
CONFIG_PACKAGE_kmod-usb-storage-extras=y
CONFIG_PACKAGE_kmod-usb-storage-uas=y

# 强制移除多余的伪 NAS 组件与高级磁盘管理 (雅典娜无需复杂磁盘阵列)
CONFIG_PACKAGE_luci-app-mini-diskmanager=n
CONFIG_PACKAGE_luci-app-partexp=n
CONFIG_PACKAGE_smartmontools=n
CONFIG_PACKAGE_smartmontools-drivedb=n
CONFIG_PACKAGE_kmod-fs-ksmbd=n
CONFIG_PACKAGE_kmod-fs-btrfs=n

# -----------------------------------------------------------------
# 3. 实用运维与界面增强
# -----------------------------------------------------------------
# 网页端终端 (折腾底层协议时，直接浏览器内敲命令，极其方便)
CONFIG_PACKAGE_luci-app-ttyd=y
# 高颜值交互界面 (目前体验最现代化的 OpenWrt 主题)
CONFIG_PACKAGE_luci-theme-argon=y
# 定时重启 (设定凌晨低谷期自动重启，作为长期运行的兜底防爆策略)
CONFIG_PACKAGE_luci-app-timedreboot=y
# 网络带宽监视器 (按设备的 MAC 地址直观统计流量，排查异常跑流必备)
CONFIG_PACKAGE_luci-app-nlbwmon=y
# 内网极限测速神器 (精准排查局域网物理带宽瓶颈)
CONFIG_PACKAGE_iperf3=y

# -----------------------------------------------------------------
# 4. 彻底剔除无用外设与杂项 (极限瘦身，强制覆盖上游默认配置)
# -----------------------------------------------------------------
# 移除所有 4G/5G 物联网模块驱动
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan=n
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan-fibocom=n
CONFIG_PACKAGE_kmod-usb-net-qmi-wwan-quectel=n
CONFIG_PACKAGE_kmod-usb-net-sierrawireless=n
# 移除所有手机 USB 共享网络通信库 (包括苹果与安卓)
CONFIG_PACKAGE_kmod-usb-net-ipheth=n
CONFIG_PACKAGE_kmod-usb-net-rndis=n
CONFIG_PACKAGE_libimobiledevice=n
CONFIG_PACKAGE_usbmuxd=n
# 移除各种外接千兆/百兆 USB 网卡驱动 (雅典娜自带 2.5G，无需外接)
CONFIG_PACKAGE_kmod-usb-net-rtl8150=n
CONFIG_PACKAGE_kmod-usb-net-rtl8152=n
CONFIG_PACKAGE_kmod-usb-net-asix=n
CONFIG_PACKAGE_kmod-usb-net-asix-ax88179=n
# 移除 M.2 NVMe 与 SATA 硬盘物理层驱动 (硬件不支持，留着纯属冗余)
CONFIG_PACKAGE_kmod-nvme=n
CONFIG_PACKAGE_nvme-cli=n
CONFIG_PACKAGE_libnvme=n
CONFIG_PACKAGE_kmod-ata-ahci=n
CONFIG_PACKAGE_kmod-ata-core=n
# 移除音频输出驱动与测分工具
CONFIG_PACKAGE_kmod-sound-core=n
CONFIG_PACKAGE_kmod-usb-audio=n
CONFIG_PACKAGE_coremark=n
# 移除集客 AC 面板控制器及传统老旧 VPN 组件
CONFIG_PACKAGE_luci-app-gecoosac=n
CONFIG_PACKAGE_kmod-ovpn-backports=n
CONFIG_PACKAGE_luci-app-attendedsysupgrade=n

# -----------------------------------------------------------------
# 5. 系统底层基础框架与高通加速网络依赖 (切勿修改)
# -----------------------------------------------------------------
CONFIG_CCACHE=y
CONFIG_DEVEL=y
CONFIG_FEED_video=n
CONFIG_TARGET_MULTI_PROFILE=y
CONFIG_TARGET_PER_DEVICE_ROOTFS=y
CONFIG_TARGET_ROOTFS_INITRAMFS=n

# 核心防火墙 (nftables) 与转发架构
CONFIG_PACKAGE_kmod-nft-arp=y
CONFIG_PACKAGE_kmod-nft-bridge=y
CONFIG_PACKAGE_kmod-nft-connlimit=y
CONFIG_PACKAGE_kmod-nft-core=y
CONFIG_PACKAGE_kmod-nft-dup-inet=y
CONFIG_PACKAGE_kmod-nft-fib=y
CONFIG_PACKAGE_kmod-nft-fullcone=y
CONFIG_PACKAGE_kmod-nft-nat=y
CONFIG_PACKAGE_kmod-nft-netdev=y
CONFIG_PACKAGE_kmod-nft-offload=y
CONFIG_PACKAGE_kmod-nft-queue=y
CONFIG_PACKAGE_kmod-nft-socket=y
CONFIG_PACKAGE_kmod-nft-tproxy=y
CONFIG_PACKAGE_kmod-nft-xfrm=y
CONFIG_PACKAGE_kmod-tun=y

# 核心系统工具
CONFIG_PACKAGE_autocore=y
CONFIG_PACKAGE_cgdisk=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_dmesg=y
CONFIG_PACKAGE_fdisk=y
CONFIG_PACKAGE_gdisk=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_ip-full=y
CONFIG_PACKAGE_luci-base=y
CONFIG_PACKAGE_luci-compat=y
CONFIG_PACKAGE_openssh-keygen=y
CONFIG_PACKAGE_openssh-sftp-server=y
CONFIG_PACKAGE_openssl-util=y
CONFIG_PACKAGE_usbutils=y

# USB 3.0 物理接口底层驱动
CONFIG_PACKAGE_kmod-usb3=y
CONFIG_PACKAGE_kmod-usb-core=y
CONFIG_PACKAGE_kmod-usb-dwc3=y
CONFIG_PACKAGE_kmod-usb-ohci=y
CONFIG_PACKAGE_kmod-usb-uhci=y
CONFIG_PACKAGE_kmod-usb-xhci=y
